---
layout: layouts/base.njk
eleventyNavigation:
  key: Home
  order: 1
---
{% for section in collections.sections %}
  <section class="{{section.data.class}}">
  <h2>{{section.data.title}}</h2>
  <p class="lede">{{section.data.description}}</p>
  {{ section.templateContent | safe }}
  </section>
{% endfor %}

